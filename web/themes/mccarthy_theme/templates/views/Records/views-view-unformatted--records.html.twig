{#
/**
 * @file
 * Unformatted row iterator for the "Records" view.
 *
 * Available variables:
 * - rows: A list of the view's row items, each with a `content` property.
 *
 * @ingroup views_templates
 */
#}
{% for row in rows %}
    {{ row.content }}
{% endfor %}
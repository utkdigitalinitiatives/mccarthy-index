{#
/**
 * @file
 * Unformatted row iterator for the "Search" view.
 *
 * Available variables:
 * - rows: A list of the view's row items, each with a `content` property.
 *
 * @ingroup views_templates
 */
#}
{% for row in rows %}
    {{ row.content }}
{% endfor %}
